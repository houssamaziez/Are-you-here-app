import 'package:app/App/View/Home/import_home.dart';

class ScreenFarot extends StatelessWidget {
  const ScreenFarot({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Container(
            decoration: const BoxDecoration(color: Colors.white),
          ),
        ],
      ),
    );
  }
}
