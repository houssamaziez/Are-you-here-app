import 'package:flutter/material.dart';

class ScreenShopping extends StatelessWidget {
  const ScreenShopping({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ScreenShopping'),
      ),
    );
  }
}
